# PromptDock RC3 Early Access 执行计划

更新日期：2026 年 8 月 1 日

## 当前基线

- 应用版本：`1.0.0`（Build `2`）
- 候选包：`PromptDock-1.0.0-rc3.dmg`
- 下载地址：`https://cuegroveapp.com/downloads/PromptDock-1.0.0-rc3.dmg`
- SHA-256：`126dd3517c3c6678a2dbbf10ac36faf90a715fab337e940e681ace926dc100c6`
- 支持范围：macOS 14+，Apple Silicon 与 Intel 通用版本
- 发布方式：Personal Team 开发签名，尚未 Developer ID 签名或 Apple 公证
- 自动验证：50 项单元测试、Debug 测试、通用 Release 构建、签名与 DMG 校验已经通过
- 数据结构：继续使用 SwiftData Schema V1；RC3 测试结束前不增加数据库字段

当前重点不是继续增加大型功能，而是证明 RC3 可以安全安装、覆盖升级、恢复数据并稳定完成核心流程。

## 阶段一：发布前闭环（1–2 天）

### 网站与审核流程

- [ ] 部署 CueGrove 最新隐私与安全说明，确认页面不再声称 PromptDock 完全不联网
- [ ] 在后台确认 Early Access 下载地址使用上方固定 HTTPS 地址
- [ ] 使用一个测试邮箱完整走通申请、管理员通知、登录审核、批准和下载邮件
- [ ] 检查邮件按钮、中文与英文内容、DMG 地址和拒绝不发信逻辑
- [ ] 删除或明确标记测试申请，避免混入真实申请统计

### 真实安装与升级

- [ ] 在未安装 PromptDock 的用户环境完成 DMG 挂载、拖入“应用程序”和首次启动
- [ ] 从 RC2 覆盖安装 RC3，确认提示词、分类、排序、自定义图片和收藏不丢失
- [ ] 确认语言、菜单栏、全局快捷键和登录启动等设置仍然有效
- [ ] 确认 Keychain 中的 AI API Key 在覆盖安装后仍可读取
- [ ] 确认退出主窗口后菜单栏快搜与全局快捷键仍可使用

### 数据安全与恢复

- [ ] 导出 JSON 备份并在另一份测试数据中执行合并导入
- [ ] 执行替换导入，确认导入前安全备份和失败回滚均有效
- [ ] 验证分类 Emoji、本地图片、顺序和提示词归属在重启后保持
- [ ] 确认 Widget 添加、复制、刷新和重启不会改变主应用数据
- [ ] 检查备份、偏好设置和日志中不存在 API Key

### AI 与模板变量

- [ ] 验证普通变量、重复变量、实时预览、添加/删除重复项和 100 项上限
- [ ] 验证未配置、无效 Key、网络超时、主动取消和服务商错误
- [ ] 使用真实 DeepSeek Key 完成一次明确确认后的成功请求
- [ ] 如有条件，验证一个用户自定义的 OpenAI 兼容 HTTPS 接口
- [ ] 通过代理或服务端日志确认请求只包含当前需求和模板语法手册
- [ ] 确认已保存提示词库、分类和本地图片没有随 AI 请求发送

出现下列任一情况，立即停止扩大测试：

- 数据丢失、数据库无法打开或覆盖升级后无法读取旧数据
- 应用持续卡死、启动失败或备份不能恢复
- Widget 操作导致主应用数据异常
- 未经用户确认就发送 AI 请求
- API Key 出现在日志、普通偏好设置或 JSON 备份中

## 阶段二：第一周 5 人测试

首批只邀请 5 人，至少覆盖：

- Apple Silicon 与 Intel
- macOS 14 与更新系统
- 从未安装、覆盖旧版本两种路径
- 使用 AI 与完全不使用 AI 两类用户

测试期间只修复数据、安全、启动、搜索、Widget、快捷键、备份和 AI 请求问题，不加入大型新功能。每个修复版本都必须：

1. 增加 Build Number。
2. 使用新的候选包名称，不覆盖旧 DMG。
3. 重新生成并发布 SHA-256。
4. 更新已知问题和升级说明。
5. 再次完成自动测试与安装冒烟测试。

反馈只收集设备、芯片、macOS 版本、复现步骤、预期结果、实际结果和出现频率。默认不要求用户提交私人提示词、分类内容或 API Key。

## 阶段三：第二周扩大到 10 人

只有第一周没有阻断问题时，才把范围扩大到最多 10 人。第二周重点验证：

- 连续日常使用中的启动和数据保持
- 提示词数量增长后的搜索、排序和快搜响应
- 菜单栏、全局快捷键与其他常用应用的冲突
- Widget 长时间运行和系统重启后的刷新
- 备份在不同设备上的往返恢复
- AI 失败、重试和取消是否始终保持可控且不泄露凭据

## RC3 稳定通过门槛

满足以下条件后，才进入下一轮产品开发：

- 连续两周没有已知数据丢失或升级损坏
- 没有启动阻断和高频崩溃
- JSON 备份、合并导入、替换导入和回滚全部通过
- RC2 覆盖 RC3 后数据、设置和 Keychain 项保持
- 主窗口、快搜、快捷键和 Widget 的核心流程可稳定完成
- AI 请求的确认步骤、发送范围和隐私说明一致
- 网站下载、申请、批准邮件和反馈入口完整可用

## 稳定后的功能开发顺序

每一步都应独立交付，并附带旧数据兼容、迁移、备份往返和失败回滚测试。

1. **抽取 PromptDockCore**  
   将搜索、模板解析、分类名称处理、备份校验等纯逻辑移入独立模块，降低界面与数据层耦合。
2. **建立 Schema V2 迁移框架**  
   保留 V1 数据，新增显式 V1 → V2 迁移和失败测试；没有迁移测试时不修改生产模型。
3. **提示词版本历史**  
   只在用户主动保存时创建版本，支持预览、比较和恢复，避免每次输入都产生历史记录。
4. **多标签系统**  
   分类继续保持单归属，标签允许多选，并支持批量添加与移除。
5. **智能集合**  
   按收藏、分类、标签、更新时间和搜索条件组合，默认只提供可解释的本地规则。
6. **稳定的模板变量元数据**  
   支持标签、默认值、顺序和重复项配置，同时兼容现有 `{{name}}` 与 `{{name[]}}`。
7. **备份格式 V2**  
   导出新元数据，同时继续支持导入 V1，并提供格式版本和校验错误说明。

## 暂缓事项

- Windows、iPhone 和 iPad 客户端
- 产品账号、云同步和多人协作
- 自动静默更新
- 由 CueGrove 统一支付所有用户的 AI 请求
- 行为分析、广告或跟踪 SDK
- RC3 稳定验证完成前的 SwiftData Schema 变更

## 分支与职责边界

- macOS 应用：`codex/promptdock-1.0.0-rc3`
- CueGrove 网站：`codex/cuegrove-site`
- 应用分支只负责 Swift、Widget、测试、CI、应用文档和发布资产
- 网站分支只负责 cuegroveapp.com 的页面、申请、审核、邮件和下载交付
- 网站更新完成后合并到应用分支，保持应用分支拥有同一网站基线；应用代码不反向混入网站分支

## 下一步

立即执行阶段一，不再打新功能包。阶段一全部通过后邀请首批 5 人；任何阻断问题都先修复、递增 Build Number 并重新生成不可覆盖的候选包。
